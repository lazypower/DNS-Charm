#!/usr/bin/python

from common import (
    sanity_check,
    install_bind,
)


# Does not assume offline environment by default
# TODO: Make offlineable with cached packages
def install():
    if sanity_check():
        install_bind()


if __name__ == '__main__':
    install()
